# Day 11 学习笔记：NumPy 与 Matplotlib 基础

本项目包含 Day 11 的学习代码，主要涵盖了 Python 数据分析库 NumPy 的基础操作以及 Matplotlib 的绘图技巧。

## 📂 文件结构

*   **`nunpy_learn.ipynb`**: NumPy 学习笔记，包含数组创建、运算、切片、形状变换及常用统计函数。
*   **`matplotlib_learn.ipynb`**: Matplotlib 绘图笔记，包含折线图、散点图、柱状图、直方图和饼图的绘制。

## 🚀 核心知识点

### 1. NumPy (`nunpy_learn.ipynb`)

*   **数组创建与属性**
    *   使用 `np.array()` 从列表创建数组。
    *   使用 `np.arange()` 生成等差数列。
    *   数组属性：`shape` (形状), `dtype` (数据类型), `ndim` (维度), `size` (元素总数)。
    *   类型转换：`astype()`。

*   **数组运算**
    *   数组与标量的运算（广播机制）。
    *   数组与数组的运算（形状需匹配或满足广播规则）。
    *   常用统计函数：`sum`, `mean`, `max`, `min`, `std` (标准差), `ptp` (极差)。
    *   累积运算：`cumsum` (累加)。

*   **索引与切片**
    *   基本切片：`t[start:end]`。
    *   多维切片：`t[行, 列]`，如 `t[:, 0:3]` 取所有行的前3列。
    *   布尔索引：`t[t > 10]`，用于条件筛选。
    *   `np.where()`：三元运算符的数组版本。

*   **数组变形与拼接**
    *   `reshape()`：改变数组形状。
    *   `append()`, `insert()`, `delete()`：增删元素。
    *   `concatenate()`, `stack()`：数组拼接与堆叠。
    *   `split()`：数组分割。

### 2. Matplotlib (`matplotlib_learn.ipynb`)

*   **基础绘图**
    *   `plt.plot()`：绘制折线图，支持设置颜色 (`color`)、线型 (`linestyle`)、线宽 (`linewidth`)。
    *   `plt.scatter()`：绘制散点图。
    *   `plt.bar()`：绘制柱状图，支持添加数值标签。
    *   `plt.hist()`：绘制直方图，用于展示数据分布。
    *   `plt.pie()`：绘制饼图，支持突出显示 (`explode`) 和百分比显示 (`autopct`)。

*   **图表定制**
    *   **画布设置**：`plt.figure(figsize=(...), dpi=...)`。
    *   **中文支持**：使用 `font_manager.FontProperties` 加载中文字体（如 STKAITI.TTF）。
    *   **轴标签与标题**：`plt.xlabel()`, `plt.ylabel()`, `plt.title()`。
    *   **刻度设置**：`plt.xticks()`, `plt.yticks()`，支持自定义刻度标签和旋转。
    *   **图例与网格**：`plt.legend()`, `plt.grid()`。

*   **子图绘制**
    *   使用 `plt.subplots(nrows, ncols)` 创建多子图布局。

## ⚠️ 常见易错点

1.  **NumPy 维度问题**：`np.append` 默认会展平数组，需指定 `axis`；`np.split` 切割时需保证能整除。
2.  **Matplotlib 中文乱码**：必须手动设置字体属性 (`fontproperties` 或 `prop`)。
3.  **函数调用**：`plt.xlabel` 是函数，不能用 `=` 赋值；`plt.figure` 不是 `plt.fig`。
4.  **直方图组数**：计算 `bins` 时建议使用 `range(min, max + distance, distance)` 确保覆盖最大值。

## 🔧 环境依赖

*   Python 3.x
*   NumPy
*   Matplotlib
*   Jupyter Notebook

---
*Happy Coding!* 🚀